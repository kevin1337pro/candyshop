{
    email {$ACME_EMAIL}
}

{$SHOP_DOMAIN} {
    encode zstd gzip
    # Avoid Apache directory redirects choosing its internal HTTP scheme/port.
    redir /wp-admin /wp-admin/ 308
    reverse_proxy wordpress:80
}
